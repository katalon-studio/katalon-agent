module.exports.JOB_STATUS = Object.freeze({
  RUNNING: 'RUNNING',
  SUCCESS: 'SUCCESS',
  FAILED: 'FAILED',
  CANCELED: 'CANCELED',
});

module.exports.NODE_STATUS = Object.freeze({
  PENDING_CANCELED: 'PENDING_CANCELED',
  CANCELED: 'CANCELED',
});
